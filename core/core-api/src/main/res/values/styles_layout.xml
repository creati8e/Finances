<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ClickableLayout">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_selection_rounded_transparent</item>
    </style>

    <style name="ToolbarIcon">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/bg_selection_borderless</item>
    </style>

    <style name="TabLayout">
        <item name="android:layout_height">36dp</item>
        <item name="android:layout_width">match_parent</item>

        <item name="android:layout_marginEnd">36dp</item>
        <item name="android:layout_marginStart">36dp</item>

        <item name="android:background">@drawable/bg_tablayout</item>

        <item name="tabIndicatorGravity">stretch</item>
        <item name="tabIndicator">@drawable/bg_tablayout_tab</item>
        <item name="tabIndicatorColor">@android:color/transparent</item>

        <item name="tabTextColor">?android:textColorPrimary</item>
        <item name="tabSelectedTextColor">?android:textColorPrimaryInverse</item>
    </style>

</resources>